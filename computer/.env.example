# Parachute Agent Configuration
# Copy this file to .env and update the values

# ===========================================
# REQUIRED
# ===========================================

# Path to your Obsidian vault (or any markdown folder)
VAULT_PATH=/path/to/your/vault

# ===========================================
# AUTHENTICATION
# ===========================================
# This server uses the Claude Agent SDK which authenticates via `claude login`.
# No API keys needed! Run `claude login` before starting the server.

# ===========================================
# OPTIONAL - Server Configuration
# ===========================================

# Port to run the server on (default: 3333)
PORT=3333

# Environment mode: development, production
NODE_ENV=production

# ===========================================
# OPTIONAL - API Authentication
# ===========================================

# API key for authenticating requests (recommended for remote access)
# When set, clients must include this in the X-API-Key header
# API_KEY=your-secret-api-key-here

# ===========================================
# OPTIONAL - CORS Configuration
# ===========================================

# Allowed origins for CORS (comma-separated, or * for all)
# Default allows localhost and common local development ports
# CORS_ORIGINS=http://localhost:3000,http://192.168.1.100:3000

# ===========================================
# OPTIONAL - Agent Defaults
# ===========================================

# Default model for agents that don't specify one
# DEFAULT_MODEL=claude-sonnet-4-20250514

# Maximum tokens for agent responses
# MAX_TOKENS=4096

# ===========================================
# OPTIONAL - MCP (Model Context Protocol)
# ===========================================

# Path to MCP servers configuration JSON file
# MCP_CONFIG_PATH=/path/to/mcp-servers.json
