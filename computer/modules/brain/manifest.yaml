name: brain
version: "3.0.0"
description: "LadybugDB embedded knowledge graph — agent-driven, no extraction pipeline"
trust_level: vault
provides:
  - BrainInterface
dependencies:
  - real-ladybug>=0.14.0
  - aiofiles>=23.0.0
